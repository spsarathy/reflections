How did viewing a diff between two versions of a file help you see the bug that was introduced?

One could see the lines that were similar and different by the appropriate markers.

How could having easy access to the entire history of a file make you a more efficient programmer in the long term?
One could recover earlier versions that might have features that might be of interest in the present.

What do you think are the pros and cons of manually choosing when to create a commit, like you do in Git, vs having versions automatically saved, like Google Docs does?
In case of a power loss, automatic saving is useful. However automatic saving creates too many versions that do not have 
logical boundaries.

Why do you think some version control systems, like Git, allow saving multiple files in one commit, while others, like Google Docs, treat each file separately?

This is because most of the projects are broken up into multiple files and they are related and work as a unit.

What happens when you initialize a repository? Why do you need to do it?
This allows me to make updates and retrieve earlier revisions
How is the staging area different from the working directory and the repository? What value do you think it offers?
Staging area allows us to keep track of the files that we want to commit ...